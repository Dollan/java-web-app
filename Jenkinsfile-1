pipeline {
  agent any
  stages {
    stage('Scan') {
      steps {
        withSonarQubeEnv(installationName: 'sq1') { 
          sh 'mvn clean package dependency-check:aggregate
              mvn sonar:sonar'
        }
      }
    }
  }
}
